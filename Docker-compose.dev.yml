services:
  backend:
    volumes:
      - .:/app
    command: >
      uvicorn app.main:app
      --host 0.0.0.0
      --port 8000
      --reload

  redis:
    ports:
      - "6379:6379"

  postgres:
    ports:
      - "5432:5432"